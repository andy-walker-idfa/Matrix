# Environment and secrets
.env

# Generated configuration files
element-web/config.json
element-call/config.json
synapse/homeserver.yaml
nginx/matrix.conf
livekit/livekit.yaml
mas/config.yaml

# Synapse secrets and keys
synapse/matrix.signing.key
synapse/*.signing.key

# MAS secrets and keys
mas/signing.key
mas/data/
mas/postgres-data/

# Data directories (local)
synapse/data/
livekit/data/

# Data directories (legacy /var/lib paths - kept for reference)
/var/lib/synapse/
/var/lib/livekit/
/var/lib/matrix/mas/

# Temporary files
*.tmp
*.log
